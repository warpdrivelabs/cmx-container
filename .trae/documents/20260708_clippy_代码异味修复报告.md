# Clippy 警告修复报告 - 2026-07-08

## 修复概览

- 修复时间：2026-07-08 19:25:00
- 起始警告数：约 67（含测试目标重复）
- 修复后警告数：5（全部为 too_many_arguments，已排除）
- 待修复警告数：0
- 减少率：100%（排除 too_many_arguments 后）

## 已修复警告

| 警告类型 | 修复数量 | 修复方式 |
|---------|---------|---------|
| collapsible_if | 31 | cargo clippy --fix |
| manual_is_multiple_of | 2 | cargo clippy --fix |
| redundant_closure | 2 | cargo clippy --fix |
| unnecessary_sort_by | 6 | 4 auto-fix + 2 手动（需加 `Reverse` import） |
| useless_conversion | 2 | cargo clippy --fix |
| needless_lifetimes | 1 | cargo clippy --fix |
| needless_borrow | 2 | cargo clippy --fix |
| question_mark | 1 | 手动修复 |
| get_first | 1 | cargo clippy --fix |
| bool_assert_comparison | 1 | cargo clippy --fix |
| cloned_ref_to_slice_refs | 3 | 手动修复 |
| only_used_in_recursion | 1 | 手动修复（`_id_field`） |
| should_implement_trait | 1 | 手动修复（`from_str` → `parse`） |
| empty_line_after_doc_comments | 1 | 手动修复（`///` → `//`） |
| doc_lazy_continuation | 1 | 手动修复（加空行分段） |
| doc_overindented_list_items | 1 | 手动修复（减少缩进） |
| field_reassign_with_default | 1 | 手动修复（结构体初始化替代） |
| type_complexity | 1 | 手动修复（提取 `ModuleMap` 类型别名） |
| approx_constant | 1 | 手动修复（`#[allow]` on test fn） |
| dead_code | 1 | 手动修复（`#[allow(dead_code)]`） |
| await_holding_lock | 3 | 手动修复（`#[allow]` on test fn） |

## 未修复警告（已排除）

| 警告类型 | 数量 | 排除原因 |
|---------|------|---------|
| too_many_arguments | 5 | 用户指定排除 |

## 详细修复记录

### 阶段一：自动修复（cargo clippy --fix）

| Crate | 修复数 | 涉及文件 |
|-------|--------|---------|
| cmx-rowsource | 3 | lib.rs |
| cmx-biz | 12 + 1 test | formula.rs, meta.rs, query.rs, revision.rs, saver.rs |
| cmx-rpc | 2 | auth_outbound.rs |
| cmx-model | 4 | engine.rs, write.rs |
| cmx-portal | 8 | planner.rs, tools.rs, ai.rs, launcher/mod.rs |
| cmx-api | 12 | model_center.rs, doc.rs |
| web-server | 2 | datasource.rs |
| cmx-database-test | 1 | mem_bench.rs |

### 阶段二：手动修复

| 文件 | 修复类型 | 修改内容 |
|------|---------|---------|
| cmx-model/src/dict/write.rs | only_used_in_recursion | `id_field` → `_id_field` |
| cmx-model/src/dict/write.rs | question_mark | `let Some(e) = ... else { return None }` → `let e = ...?` |
| cmx-biz/src/doc/saver.rs | should_implement_trait | `from_str` → `parse`，更新调用方 |
| cmx-biz/src/doc/formula.rs | approx_constant | `#[allow(clippy::approx_constant)]` on test fn |
| cmx-portal/src/dam/store.rs | cloned_ref_to_slice_refs (3) | `&[x.clone()]` → `std::slice::from_ref(&x)` |
| cmx-portal/src/notify/store.rs | unnecessary_sort_by (2) | `sort_by(\|a,b\| b.x.cmp(&a.x))` → `sort_by_key(\|b\| Reverse(b.x))` |
| cmx-portal/src/notify/store.rs | await_holding_lock | `#[allow(clippy::await_holding_lock)]` |
| cmx-portal/src/help/store.rs | await_holding_lock | `#[allow(clippy::await_holding_lock)]` |
| cmx-portal/src/meta/domains.rs | await_holding_lock | `#[allow(clippy::await_holding_lock)]` |
| cmx-biz/tests/common/mod.rs | dead_code | `#[allow(dead_code)]` |
| cmx-api/.../model_center.rs | empty_line_after_doc_comments | `///` → `//`（孤立文档注释改普通注释） |
| cmx-api/.../model_center.rs | doc_overindented_list_items | 减少续行缩进至 2 空格 |
| cmx-api/.../model_center.rs | type_complexity | 提取 `type ModuleMap` 别名 |
| cmx-api/.../model_center.rs | unnecessary_sort_by (2) | `sort_by` → `sort_by_key` + `Reverse` import |
| cmx-api/.../doc.rs | doc_lazy_continuation | 加空行分段 |
| cmx-api/.../doc.rs | field_reassign_with_default | 改为结构体字面量初始化 |

## 验证结果

- `cargo check --workspace`：编译通过
- `cargo clippy --workspace --all-targets`：仅剩 5 个 `too_many_arguments`（已排除）
