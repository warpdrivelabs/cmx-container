# 计划：编写 cmx-auth 模块使用与配置指南

> 日期：2026-06-16 | 模块：`cmx-auth` | 任务：编写面向使用者/运维者的完整使用与配置文档

***

## 一、目标

在 `crates/libs/cmx-infra/cmx-auth/` 目录下新建一份 `USAGE.md`（使用与配置指南），全面介绍 cmx-auth 模块的使用方法、配置项说明、典型场景、运维操作。**不包含代码**，纯说明性文档。

参考来源：

* 源码：`crates/libs/cmx-infra/cmx-auth/src/`（auth_service_impl.rs / config.rs / lib.rs 等）
* 配置手册：`config/CONFIG_MANUAL.md`（已有详细配置项字典）
* 设计文档 1：`.trae/documents/20260615_cmx-auth_企业级统一认证模块架构方案.md`
* 设计文档 2：`.trae/documents/20260615_cmx-auth_第三方OAuth2Provider对接方案.md`
* 路由白名单：`crates/libs/cmx-api/src/middleware/mw_auth.rs` 的 `AUTH_WHITELIST`
* API 端点：`crates/libs/cmx-api/src/handlers/auth/handler.rs` 和 `oauth2_handler.rs`

***

## 二、文档结构设计

### 1. 文档元数据

* **路径**：`crates/libs/cmx-infra/cmx-auth/USAGE.md`
* **文件名**：`USAGE.md`（README.md 已存在的可能性低，选 USAGE 避免冲突；后续若 README 不存在可改名 README.md）
* **目标读者**：平台使用者、运维人员、二次开发集成方
* **目标**：5-30 分钟内让读者完成 ① 快速接入 ② 完成典型配置 ③ 排查常见问题

### 2. 章节大纲（13 章）

| 章节 | 标题 | 关键内容 |
|------|------|---------|
| 一 | 模块概述 | cmx-auth 能力清单（JWT 双令牌 / Argon2 / OAuth2 授权码 / 第三方登录 / 会话 / API Key / 密钥轮换）、不直接依赖 cmx-iam、Token 通过 Authorization Header |
| 二 | 核心概念 | 双令牌机制（Access + Refresh）、策略模式（Credentials 枚举）、Token 生命周期、会话 vs Token 区别、AuthContext 注入流程 |
| 三 | 快速开始 | 5 步走：① 引入配置 ② 启动 cmx-auth ③ 客户端登录获取 Token ④ 调用受保护 API ⑤ 刷新 Token |
| 四 | 认证模式 | 4 种模式详细说明：用户名密码、API Key、OAuth2 Authorization Code、第三方 OAuth2 Provider |
| 五 | 配置详解 | 11 个配置段逐项说明：jwt / token / argon2 / session / cache / oauth2 / account_link / providers / super_admin / static_api_keys / 完整示例 |
| 六 | 路由白名单 | 默认白名单列表、自定义白名单位置（`mw_auth.rs` 的 `AUTH_WHITELIST`）、白名单匹配规则（starts_with 前缀匹配）、添加新白名单的步骤 |
| 七 | 第三方 OAuth2 对接 | 4 个步骤走通：① 在 Provider 开发者后台注册应用 ② 配置 client_id/secret/redirect_uri ③ 启动后 list_providers 可见 ④ 前端发起授权 → 回调 → 换 Token；Google / GitHub / GitLab 三个典型案例 |
| 八 | API 端点参考 | 完整列出 18+ 个端点（登录/刷新/登出/校验/撤销/心跳/改密/OAuth2 authorize/login/token/Provider 列表/回调/换 Token/绑定/解绑/健康检查），含请求方法、路径、是否需要认证、用途 |
| 九 | 数据库表 | 相关 6 张表清单：`cmx_user` / `cmx_auth_client` / `cmx_auth_token_record` / `cmx_auth_api_key` / `cmx_auth_password_history` / `cmx_auth_oauth2_account`，每张表说明用途和关键字段 |
| 十 | Redis Key 设计 | 12 个 Key 模式清单（refresh / session / blacklist / online / oauth2 csrf / oauth2 authcode / oauth2 provider state / oauth2 provider callback / login_fail / locked），含 Hash Tag 说明和 TTL |
| 十一 | 安全机制 | 9 大机制：时序攻击防护、密码策略与历史、Token Rotation 重放检测、CSRF state 原子消费、PKCE、Open Redirect 防护、ID Token 签名验证、账号锁定、最后绑定不可解绑 |
| 十二 | 可观测性 | Prometheus 指标（6 个 Counter/Gauge/Histogram）、健康检查端点、Tracing span 结构、审计日志（cmx-audit 集成） |
| 十三 | 运维与排障 | 常见问题 8-10 个：忘记超管密码、强制下线某个用户、轮换 JWT 密钥、撤销所有 Token、清理过期会话、Redis 缓存重建、性能调优（Argon2 参数）、集群模式 Hash Tag 注意事项 |

### 3. 重点突出项

* **白名单配置**：从源码 `mw_auth.rs` 提取完整白名单列表 + 匹配机制 + 自定义方法
* **OAuth2 集成**：分"自建 Authorization Server"和"对接第三方 Provider"两条主线，前者给客户端开发者看，后者给运维配置者看
* **典型配置示例**：包含 Google / GitHub / GitLab（generic）三个完整可用的 TOML 片段
* **运维操作清单**：每条命令/操作说明"在哪做、做什么、注意事项"

### 4. 不包含的内容

* **不包含代码**（用户明确要求）
* 不包含内部 Rust 结构体定义（仅在概念章节用文字描述字段语义）
* 不包含 SQL 建表语句（仅说明表名和用途，详细 DDL 引用架构方案文档）

***

## 三、关键来源映射

| 文档章节 | 主要来源 |
|----------|---------|
| 概述 / 核心概念 | 架构方案 §1, §2.1, §2.2 |
| 快速开始 / API 端点 | `auth/handler.rs` + `oauth2_handler.rs` + `mw_auth.rs` |
| 认证模式 | 架构方案 §3 + OAuth2 对接方案 §2.2 时序图 |
| 配置详解 | `config.rs` + `config_template.toml` + `CONFIG_MANUAL.md` §认证配置 |
| 路由白名单 | `mw_auth.rs` `AUTH_WHITELIST` 常量 |
| 第三方 OAuth2 | OAuth2 对接方案 §3.3, §3.4, §3.5, §4.3 |
| 数据库表 | 架构方案 §4.4 |
| Redis Key | 架构方案 §4.3 |
| 安全机制 | 架构方案 §5 + OAuth2 对接方案 §11 |
| 可观测性 | `metrics.rs` + 架构方案 §6 |
| 运维排障 | 架构方案 §8.4（Pub/Sub 失效）+ 实操经验 |

***

## 四、验证步骤

1. **完整性核对**：与 `CONFIG_MANUAL.md` 现有 `[auth.*]` 配置段一一对照，确保无遗漏
2. **OAuth2 章节准确性**：核对 `OAuth2ProviderConfig` 字段（`config.rs`）与文档描述一致
3. **API 路径核对**：与 `mw_auth.rs` 白名单 + `auth/handler.rs` 路径前缀 + `oauth2_handler.rs` 端点对照
4. **可读性检查**：目录结构清晰、章节锚点可跳转、典型示例完整可复制

***

## 五、约束

* **纯说明文档，不含代码块**（用户明确要求）
* 沿用项目规范：使用中文、Markdown 格式、相对路径引用源码
* 不创建冗余文件，输出到 `crates/libs/cmx-infra/cmx-auth/USAGE.md` 一个文件即可
* 文档语言：中文（与项目其他文档一致）
* 计划文件不动，仅生成目标 USAGE.md

***

## 六、风险与应对

| 风险 | 应对 |
|------|------|
| 文档可能与代码实际行为有出入 | 严格基于已读源码（auth_service_impl.rs / config.rs / mw_auth.rs 等）编写，避免引用未确认的二手信息 |
| 配置项过多导致文档臃肿 | 每个配置项配"作用 + 默认值 + 使用建议 + 示例"四要素，不展开实现细节 |
| 第三方 OAuth2 流程描述过于复杂 | 用步骤化（①②③）+ 关键点（防 Open Redirect / 自动关联策略）突出重点 |
| 白名单列表可能变化 | 在文档中标注"以源码 `mw_auth.rs` 的 `AUTH_WHITELIST` 为准"，并说明白名单修改位置 |
