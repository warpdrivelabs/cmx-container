# 查重接口实现计划

> **目标:** 为 plugin、table_metadata、service 三个模块各增加一个查重接口，根据唯一字段查询数据是否存在。

## 需求分析

| 模块 | 查询字段 | 数据库方法 | 返回值 |
|------|----------|-----------|--------|
| plugin | plugin_id | `PluginRepository::plugin_exists(plugin_id)` | "1" 存在, "0" 不存在 |
| table_metadata | table_name | `TableMetadataService::get_by_table_name` | "1" 存在, "0" 不存在 |
| service | service_key | `ServiceQuery::get_service(service_key)` | "1" 存在, "0" 不存在 |

## API 设计

| 接口 | 方法 | 路径 | 参数 |
|------|------|------|------|
| 插件查重 | GET | `/api/plugin/exists?plugin_id=xxx` | Query: plugin_id |
| 表元数据查重 | GET | `/api/table-metadata/exists?table_name=xxx` | Query: table_name |
| 服务查重 | GET | `/api/service/exists?service_key=xxx` | Query: service_key |

**响应格式:** `ApiResp<String>`，code=0，data="1" 或 "0"

---

## 实现步骤

### Task 1: plugin 模块增加 exists 接口

**文件:**
- 修改: `crates/libs/cmx-api/src/handlers/plugin/handler.rs`
- 修改: `crates/libs/cmx-api/src/handlers/plugin/mod.rs`

**Step 1: 添加 handler 函数**

在 `handler.rs` 末尾添加:

```rust
/// 查询插件是否存在
///
/// 处理 GET /api/plugin/exists 请求，通过 plugin_id 查询插件是否已存在。
///
/// # 参数
/// - `query`: 查询参数（PluginExistsQuery）
///
/// # 查询参数
/// - `plugin_id`: 插件ID
///
/// # 响应体
/// - code: 0
/// - data: "1" 存在, "0" 不存在
#[utoipa::path(
    get,
    path = "/api/plugin/exists",
    params(PluginExistsQuery),
    responses(
        (status = 200, description = "查询成功", body = ApiResp<String>)
    ),
    tag = "Plugin"
)]
pub async fn plugin_exists(
    Query(query): Query<PluginExistsQuery>,
) -> Result<Json<ApiResp<String>>, Error> {
    let manager = cmx_plugin::GlobalPluginManager::get();
    let exists = manager.repository().plugin_exists(&query.plugin_id).await
        .map_err(|e| Error::internal_error(format!("查询插件存在性失败: {}", e)))?;

    Ok(Json(ApiResp::ok(if exists { "1" } else { "0" }.to_string())))
}
```

**Step 2: 添加请求结构体**

在 `crates/libs/cmx-api/src/handlers/plugin/request.rs` 添加:

```rust
/// 插件查重查询参数
#[derive(Debug, Clone, Deserialize, IntoParams)]
pub struct PluginExistsQuery {
    /// 插件ID
    pub plugin_id: String,
}
```

**Step 3: 导出 handler**

在 `mod.rs` 中:
- 在 `use handler::` 行添加 `plugin_exists`
- 在 `fn inner_routes()` 添加 `.route("/exists", get(plugin_exists))`

---

### Task 2: table_metadata 模块增加 exists 接口

**文件:**
- 修改: `crates/libs/cmx-api/src/handlers/table_metadata/handler.rs`
- 修改: `crates/libs/cmx-api/src/handlers/table_metadata/mod.rs`

**Step 1: 添加 handler 函数**

在 `handler.rs` 末尾添加:

```rust
/// 查询表元数据是否存在
///
/// 处理 GET /api/table-metadata/exists 请求，通过 table_name 查询表是否已存在。
///
/// # 参数
/// - `query`: 查询参数（TableMetadataExistsQuery）
///
/// # 查询参数
/// - `table_name`: 表名称
///
/// # 响应体
/// - code: 0
/// - data: "1" 存在, "0" 不存在
#[utoipa::path(
    get,
    path = "/api/table-metadata/exists",
    params(TableMetadataExistsQuery),
    responses(
        (status = 200, description = "查询成功", body = ApiResp<String>)
    ),
    tag = "TableMetadata"
)]
pub async fn table_metadata_exists(
    State(_cmx_state): State<CmxAppState>,
    CmxSvrContext(_svr_ctx): CmxSvrContext,
    headers: HeaderMap,
    Query(params): Query<TableMetadataExistsQuery>,
) -> Result<Json<ApiResp<String>>, Error> {
    let mm = get_default_db_manager();
    let db_id = get_db_id_from_header(&headers).await;

    let dataset = TableMetadataService::get_by_table_name(mm, &db_id, &params.table_name, None)
        .await
        .map_err(|e| Error::internal_error(format!("查询表存在性失败: {}", e)))?;

    let exists = !dataset.rows.is_empty();
    Ok(Json(ApiResp::ok(if exists { "1" } else { "0" }.to_string())))
}

/// 根据 table_name 查询表元数据的查询参数
#[derive(Debug, Clone, Serialize, Deserialize, IntoParams)]
pub struct TableMetadataExistsQuery {
    /// 表名称
    pub table_name: String,
}
```

**Step 2: 路由注册**

在 `mod.rs` 的 `fn inner_routes()` 中添加:
`.route("/exists", get(handler::table_metadata_exists))`

---

### Task 3: service 模块增加 exists 接口

**文件:**
- 修改: `crates/libs/cmx-api/src/handlers/service/handler.rs`
- 修改: `crates/libs/cmx-api/src/handlers/service/models.rs`
- 修改: `crates/libs/cmx-api/src/handlers/service/mod.rs`

**Step 1: 添加查询结构体**

在 `models.rs` 中添加:

```rust
/// 服务查重查询参数
#[derive(Debug, Clone, Serialize, Deserialize, IntoParams)]
pub struct ServiceExistsQuery {
    /// 服务唯一标识
    pub service_key: String,
}
```

**Step 2: 添加 handler 函数**

在 `handler.rs` 末尾添加:

```rust
/// 查询服务是否存在
///
/// 处理 GET /api/service/exists 请求，通过 service_key 查询服务是否已存在。
///
/// # 参数
/// - `state`: 应用状态
/// - `query`: 查询参数（ServiceExistsQuery）
///
/// # 查询参数
/// - `service_key`: 服务唯一标识
///
/// # 响应体
/// - code: 0
/// - data: "1" 存在, "0" 不存在
#[utoipa::path(
    get,
    path = "/api/service/exists",
    params(ServiceExistsQuery),
    responses(
        (status = 200, description = "查询成功", body = ApiResp<String>)
    ),
    tag = "Service"
)]
pub async fn service_exists(
    State(state): State<CmxAppState>,
    Query(query): Query<ServiceExistsQuery>,
) -> Result<Json<ApiResp<String>>, Error> {
    let service_query: &Arc<dyn ServiceQuery> = state.service_query()
        .ok_or_else(|| Error::internal_error("服务查询器未初始化"))?;

    let service = service_query.get_service(&query.service_key).await
        .map_err(|e| Error::internal_error(format!("查询服务存在性失败: {}", e)))?;

    let exists = service.is_some();
    Ok(Json(ApiResp::ok(if exists { "1" } else { "0" }.to_string())))
}
```

**Step 3: 导出和路由注册**

在 `mod.rs` 中:
- 添加 `service_exists` 到导出列表
- 在 `fn inner_routes()` 中添加 `.route("/exists", get(service_exists))`

---

### Task 4: 验证编译

```bash
cargo check --package cmx-api
```

预期: 无编译错误

---

### Task 5: 提交代码

```bash
git add .
git commit -m "feat: 新增 plugin/table_metadata/service 查重接口

- plugin: GET /api/plugin/exists?plugin_id=xxx
- table_metadata: GET /api/table-metadata/exists?table_name=xxx
- service: GET /api/service/exists?service_key=xxx

返回 ApiResp<String>，code=0，data='1' 存在，data='0' 不存在"
```
