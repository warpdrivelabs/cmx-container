[package]
name = "cmx-wasmdemo"
version = "0.1.0"
edition = "2021"
description = "WASM demo module for testing host functions"
authors = ["skylake@pansoft.com"]

[lib]
crate-type = ["cdylib"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
